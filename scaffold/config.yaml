site:
  title: My Garden
  description: A digital garden built with mug.
  # Origin used for absolute URLs in feeds and sitemaps. Edit before publishing.
  url: https://example.com
  # Subpath the site is hosted under, e.g. "/notes". Leave empty for root.
  base_path: ""

# Layer the bundled digital-garden theme: it supplies the templates, styles, and
# config defaults (the `notes`/`all` collections and the default note layout).
# Edit its files under themes/obsidian/, or override here — your config wins.
theme: themes/obsidian
