site:
  title: My Site
  description: A test site
collections:
  posts:
    path: "posts/*.md"
defaults:
  posts:
    permalink: /blog/:yyyy/:slug/

# Keep this fixture focused: auto-generated sitemap/feed are tested separately.
sitemap:
feed: []
